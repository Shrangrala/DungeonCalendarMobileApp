Dungeon Calendar Mobile Native Layout Fix

Fixed:
- Bottom navigation now has extra bottom padding so it sits above Android phone navigation buttons.
- Cogwheel moved down away from the phone status/notification area.
- Settings cogwheel opens the new native modal menu instead of routing to the old mobile app layout.
- Bottom buttons and quick-action buttons now navigate only to new native screens.
- App.js simplified to use DungeonCalendarMobileApp directly.
- Logo is loaded from assets/dungeon-calendar-logo.png.

Build:
1. npm install --legacy-peer-deps
2. cd android
3. .\gradlew.bat bundleRelease
